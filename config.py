"""
定数・設定値をまとめたモジュール。

「プロンプトの外出し」と同じ発想で、コード全体に散らばりがちなマジックナンバー・
設定値を1箇所に集約している。値を調整したいときはこのファイルだけ見ればよい。
"""

import os

# ------------------------------------------------------------------
# パス関連
# ------------------------------------------------------------------
PDF_DIR = "pdf_images"
CHROMA_DB_PATH = "./chroma_db"
SESSION_FILE = "chat_sessions.json"
ANSWER_CACHE_FILE = "answer_cache.json"  # 完全一致キャッシュの保存先
DB_PATH = "jarvis.db"  # SQLite統合データベース（chat_sessions/answer_cache/memos）
MEMO_FILE = "memos.json"  # tools.pyのadd_memo/list_memosが使うメモ・ToDoの保存先
DB_PATH = "jarvis.db"  # SQLite統合DB（chat_sessions/answer_cache/memosを集約。Step 2で導入）
LOG_FILE = "app.log"
PROMPTS_DIR = os.path.join(os.path.dirname(__file__), "prompts")

# ------------------------------------------------------------------
# モデル関連
# ------------------------------------------------------------------
EMBEDDING_MODEL_NAME = "intfloat/multilingual-e5-small"
# 2026年8月時点ではGemini 3系（例: gemini-3.5-flash）も利用可能。
# 関数呼び出しの精度を上げたい場合は切り替えを検討してください（要動作確認）。
GEMINI_MODEL_NAME = "gemini-2.5-flash"

# multilingual-e5 系モデルは "query: " / "passage: " のプレフィックスを付けて
# 埋め込みを取ることが公式に推奨されている。付けないと検索精度が本来より落ちる。
EMBEDDING_QUERY_PREFIX = "query: "
EMBEDDING_PASSAGE_PREFIX = "passage: "

# 検索結果の再ランキングに使うCrossEncoder（sentence-transformersに同梱、追加課金なし）
RERANK_MODEL_NAME = "cross-encoder/mmarco-mMiniLMv2-L12-H384-v1"

# ------------------------------------------------------------------
# ChromaDBコレクション名
# ------------------------------------------------------------------
PDF_CHILD_COLLECTION_NAME = "modular_rag_children_db"
PDF_PARENT_COLLECTION_NAME = "modular_rag_parents_db"
CHAT_COLLECTION_NAME = "chat_memory_db_v2"  # v2: e5プレフィックス+コサイン距離化に伴い名称を変更
# （コレクションの距離指標(hnsw:space)は作成時に固定され、同名の既存コレクションを
#  get_or_create_collectionで開くと古い設定のまま再利用されてしまうため、
#  コサイン距離への移行に合わせてコレクション名を変えて確実に新設定で作り直す。
#  ＝この更新を適用すると、過去に保存した長期記憶は一度リセットされます。）

# e5はコサイン類似度で学習・評価されているモデルなので、Chroma側の距離指標も
# コサインに合わせる（未指定だとChromaのデフォルトであるL2距離になってしまう）。
CHROMA_DISTANCE_METRIC = "cosine"

# ------------------------------------------------------------------
# チャンク分割
# ------------------------------------------------------------------
PARENT_CHUNK_SIZE = 1000
CHILD_CHUNK_SIZE = 200
PARENT_CHUNK_OVERLAP = 100   # 親チャンクの境界で文脈が途切れるのを防ぐための重複幅
CHILD_CHUNK_OVERLAP = 40     # 子チャンクも同様に少し重ねて検索漏れを減らす
MIN_CHUNK_LENGTH = 20        # 短すぎるチャンクを弾く閾値

# ------------------------------------------------------------------
# 検索関連
# ------------------------------------------------------------------
PDF_SEARCH_OVERFETCH = 12        # リランキング前に多めに取得する件数
PDF_SEARCH_MAX_PARENTS = 3       # 採用するユニークな親チャンクの上限
MEMORY_SEARCH_N_RESULTS = 2      # 長期記憶から拾う件数（以前は1件のみだった）

THRESHOLD_OPTIONS = {"厳重 (0.2)": 0.2, "標準 (0.3)": 0.3, "緩め (0.5)": 0.5}
DEFAULT_THRESHOLD_LABEL = "標準 (0.3)"
# 距離指標をL2→コサインに変更したため、この閾値が実際に効いているかは
# 手元のデータで再確認してください（コサイン距離は0〜2のレンジで、
# 似た文書同士は概ね0.1〜0.4程度に収まることが多いです）。

# ------------------------------------------------------------------
# チャット履歴（トークン基準）
# ------------------------------------------------------------------
CHAT_HISTORY_MAX_TOKENS = 1500  # 直近の会話履歴として渡す概算トークン数の上限
CHAT_HISTORY_MAX_MESSAGES = 20  # 念のための件数上限（暴走防止）

# ------------------------------------------------------------------
# 図表ページのマルチモーダル解析
# ------------------------------------------------------------------
MULTIMODAL_BATCH_SIZE = 8        # 何ページ分をまとめて1回のAPI呼び出しにするか
MULTIMODAL_MAX_PARALLEL_BATCHES = 3  # バッチ呼び出しを同時に何本まで並列化するか
# 画像・図形要素があっても、通常のテキスト抽出でこの文字数以上取れていれば
# 「ロゴ程度の埋め込み画像」とみなしGemini Visionには回さない（コスト削減）。
TEXT_SUFFICIENCY_THRESHOLD_CHARS = 200

# ------------------------------------------------------------------
# Gemini API 呼び出しの安定化
# ------------------------------------------------------------------
RETRY_MAX_ATTEMPTS = 4
RETRY_BASE_DELAY_SEC = 2.0   # 指数バックオフの初期待機秒数（2, 4, 8, 16...）
RETRY_MAX_DELAY_SEC = 30.0

# ------------------------------------------------------------------
# 長期記憶
# ------------------------------------------------------------------
MEMORY_CONTEXT_MAX_CHARS = 800  # 1件あたりの保存文字数の上限（ノイズ肥大化を防ぐ簡易対策）

# ------------------------------------------------------------------
# Jarvis Core（常駐プロセス）
# ------------------------------------------------------------------
CORE_LOG_FILE = "jarvis_core.log"     # Streamlit側のapp.logとは別ファイルに分離
CORE_PID_FILE = "jarvis_core.pid"     # 二重起動防止用
CORE_HEARTBEAT_INTERVAL_SEC = 60      # 生存確認ログの間隔（Step 4でスケジューラのtickに置き換え予定）
CORE_TASK_NAME = "JarvisCore"         # Windowsタスクスケジューラ上のタスク名

# ------------------------------------------------------------------
# Jarvis Core ⇔ UI 間のローカルAPI
# ------------------------------------------------------------------
CORE_API_HOST = "127.0.0.1"   # ローカル専用。外部からの接続は受け付けない
CORE_API_PORT = 8765
CORE_API_TIMEOUT_SEC = 1.0    # UI側がCoreの生死確認をする際のタイムアウト（体感速度を優先し短めに）

# ------------------------------------------------------------------
# ツール呼び出し（関数呼び出し）
# ------------------------------------------------------------------
TOOL_CALL_MAX_ITERATIONS = 5  # 1回の回答生成で許容するツール呼び出しの往復回数の上限（暴走防止）